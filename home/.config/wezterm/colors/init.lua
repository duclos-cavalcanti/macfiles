-- Palette loader: loads the active base16 palette into a table and returns it.
-- To switch themes, drop another palette in this folder
-- (e.g. colors/gruvbox.lua) and change the require target below.

local M = require("colors.adwaita")

return M
