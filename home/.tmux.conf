# General
set -g prefix C-a
bind C-a send-prefix
set -g mouse on
set -s escape-time 1
set -g base-index 1
set -g pane-base-index 1
set -g display-panes-time 3000
set -g focus-events on
set -g automatic-rename off
setw -g mode-keys vi

bind r source-file ~/.tmux.conf \; display-message "reloaded config!"

# Hooks
set-hook -g client-focus-in 'resize-window -A'
set-hook -g client-attached 'resize-window -A'
set-hook -g client-session-changed 'resize-window -A'
set-hook -g client-resized 'resize-window -A'

# Copy mode
unbind y
unbind [
bind y copy-mode
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi 'y' send -X copy-pipe-and-cancel 'reattach-to-user-namespace pbcopy'
bind -T copy-mode-vi Enter send -X copy-pipe-and-cancel 'reattach-to-user-namespace pbcopy'

# Panes — navigation
bind h select-pane -L
bind l select-pane -R
bind k select-pane -U
bind j select-pane -D
bind z resize-pane -Z
bind m select-pane -m
unbind '"'
bind '"' select-pane -t '~'

# Panes — splitting
unbind %
bind v split-window -h
bind s split-window

# Panes — resizing
bind -r down  resize-pane -D
bind -r up    resize-pane -U
bind -r left  resize-pane -L
bind -r right resize-pane -R
bind C-l resize-window -A

# Panes — management
bind x kill-pane

# Windows — navigation
bind -r b select-window -t :-
bind -r n select-window -t :+
bind -r -n M-h select-window -t :-
bind -r -n M-l select-window -t :+

# Windows — reordering
bind -r B swap-window -t -1 \; previous-window
bind -r N swap-window -t +1 \; next-window
bind -r -n M-H swap-window -t -1 \; previous-window
bind -r -n M-L swap-window -t +1 \; next-window

# Sessions
bind X kill-session
bind -r L switch-client -l
bind -r ) switch-client -n
bind -r ( switch-client -p

# Scripts
bind Enter run-shell "~/.tmux/terminal.sh"
bind Space command-prompt -p "session name:" "run-shell '~/.tmux/break.sh %%'"
bind Y run-shell "~/.tmux/yank.sh"
bind C run-shell "~/.tmux/launch.sh"
bind W run-shell "~/.tmux/sessions.sh"
bind G run-shell "~/.tmux/worktree.sh"
bind D run-shell "~/.tmux/worktree.sh delete"
bind E run-shell "~/.tmux/scratch.sh"

# Appearance — panes
set -g pane-border-style 'fg=white'
set -g pane-active-border-style 'fg=blue'
set -g window-style 'bg=colour236'
set -g window-active-style 'bg=colour235'

# Appearance — status bar
bind t set -g status
set -g status on
set -g status-interval 2
set -g status-justify left
set -g status-position top
set -g status-style 'fg=white,bg=default,bold'
set -g status-left ' #S:'
set -g status-left-style 'fg=red'
set -g status-right-length 120
set -g status-right "#(~/.tmux/statusline_right.sh)"
set -g status-right-style 'fg=green'
set -g message-style 'fg=default'
set -g message-command-style 'fg=default'

# Appearance — window status
setw -g window-status-format " #I:#W "
setw -g window-status-current-format " #I:#W#{?pane_marked,+,*}#{?window_zoomed_flag, Z,}"
setw -g window-status-style 'fg=brightblack'
setw -g window-status-current-style 'fg=white'
